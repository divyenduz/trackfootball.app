pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm run lint
      tags:
        - linting
        - typescript
    update-lockfile:
      glob: '{**/package.json,pnpm-workspace.yaml}'
      run: pnpm install
      stage_fixed: true
