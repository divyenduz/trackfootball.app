{
  "name": "@trackfootball/postgres",
  "version": "0.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "build": "echo 'No build step required' && exit 0",
    "test": "echo 'No tests' && exit 0",
    "lint": "tsc --noEmit",
    "pg:dump": "command -v pg_dump && PGPASSWORD=postgres pg_dump -d `cat ../../.dburl` --schema-only --no-owner > ./schema.sql || echo 'pg_dump not found'"
  },
  "devDependencies": {
    "@types/geojson": "7946.0.16",
    "@types/node": "22.19.9",
    "typescript": "5.9.2"
  },
  "dependencies": {
    "geojson": "0.5.0",
    "zod": "3.25.64",
    "postgres": "3.4.7",
    "tiny-invariant": "1.3.3"
  }
}
